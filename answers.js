function sortByStrings(s, t) {

}

function decodeString(s) {
  let output = [];
  const integers = ['1', '2', '3', '4', '5',
                    '6', '7', '8', '9'];

  function recursion(s) {
   //remove layer of brackets.
      //remainder = split[1].slice(0,-1);

   //separate integers from characters  in split[0]

  //return integer * recursion(remainder)
  }

  return recursion(s);
}
